# tzdata: python:3.11-slim bringt keine Zeitzonendaten mit. ib_insync parst
# IB-Zeitstempel mit ZoneInfo('US/Eastern'); ohne tzdata schlaegt das mit
# "ZoneInfoNotFoundError: No time zone found with key US/Eastern" fehl und der
# reqExecutions-Replay beim Verbindungsaufbau verliert Ausfuehrungen — genau der
# Pfad, der Positionen ohne lokalen FILL-Record erzeugt.
tzdata==2025.2
ib_insync==0.9.86
supabase==2.5.0
python-dotenv==1.0.1
aiohttp==3.9.5
