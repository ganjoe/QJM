FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir "websockets>=12.0" "msgspec>=0.18.0"

COPY src/ /app/src/
COPY run_server.py /app/run_server.py

EXPOSE 8765 8766

ENV PYTHONUNBUFFERED=1

CMD ["python3", "run_server.py"]
